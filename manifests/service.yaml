apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: Nodeport
  selector:
    app.kubernetes.io/name: node-argocd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
